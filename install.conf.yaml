- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.clojure/deps.edn: 
       create: true
       path: clojure/deps.edn
    ~/.config/git/config:
       create: true
       path: git/config
