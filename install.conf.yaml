- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.clojure/deps.edn: 
    create: true
      path: clojure/deps.edn

- shell:
    - [git submodule update --init --recursive, Installing submodules]

